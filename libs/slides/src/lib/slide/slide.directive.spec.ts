import { SlideDirective } from './slide.directive';

describe('SlideDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideDirective();
    expect(directive).toBeTruthy();
  });
});
