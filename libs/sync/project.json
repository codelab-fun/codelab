{
  "name": "sync",
  "projectType": "library",
  "sourceRoot": "libs/sync/src",
  "prefix": "codelab",
  "targets": {
    "build": {
      "executor": "@nrwl/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/sync/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/sync/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/sync/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/sync/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/sync/**/*.ts", "libs/sync/**/*.html"]
      }
    }
  },
  "tags": []
}
