{
  "projectType": "library",
  "sourceRoot": "libs/firebase-login/src",
  "prefix": "codelab",
  "targets": {
    "build": {
      "executor": "@nrwl/angular:package",
      "outputs": [
        "{workspaceRoot}/dist/{projectRoot}"
      ],
      "options": {
        "project": "libs/firebase-login/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/firebase-login/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/firebase-login/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "libs/firebase-login/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "libs/firebase-login/**/*.ts",
          "libs/firebase-login/**/*.html"
        ]
      }
    }
  },
  "tags": []
}
