import { HooksDirective } from './hooks.directive';

describe('HooksDirective', () => {
  it('should create an instance', () => {
    const directive = new HooksDirective();
    expect(directive).toBeTruthy();
  });
});
