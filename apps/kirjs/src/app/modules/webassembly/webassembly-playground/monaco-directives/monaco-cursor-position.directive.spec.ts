import { MonacoCursorPositionDirective } from './monaco-cursor-position.directive';

describe('MonacoCursorPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new MonacoCursorPositionDirective();
    expect(directive).toBeTruthy();
  });
});
