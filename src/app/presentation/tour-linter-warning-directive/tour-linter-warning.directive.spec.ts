import { TourLinterWarningDirective } from './tour-linter-warning.directive';

describe('TourLinterWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new TourLinterWarningDirective();
    expect(directive).toBeTruthy();
  });
});
