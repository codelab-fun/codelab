import { SlideIfDirective } from './slide-if.directive';

describe('SlideIfDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideIfDirective();
    expect(directive).toBeTruthy();
  });
});
