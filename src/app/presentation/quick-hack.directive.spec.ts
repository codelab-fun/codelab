import { QuickHackDirective } from './quick-hack.directive';

describe('QuickHackDirective', () => {
  it('should create an instance', () => {
    const directive = new QuickHackDirective();
    expect(directive).toBeTruthy();
  });
});
